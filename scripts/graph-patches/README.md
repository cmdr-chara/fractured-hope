# Graph Patch Helpers

These CommonJS scripts are one-off maintenance helpers used while tuning the
Quartz graph plugin cache under `.quartz/plugins/graph`.

The `.quartz` directory is ignored because it is generated plugin output, so
these scripts document the local graph experiments without committing generated
plugin files. They are not part of the normal build.
